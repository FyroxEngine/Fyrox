// GEN-ATOM-06: Collision Resolution Engine (Bevy Rapier integration — phase 2)
use bevy::prelude::*;

pub struct CollisionPlugin;
impl Plugin for CollisionPlugin {
    fn build(&self, _app: &mut App) {}
}
